/**
 * Create a function named setFromArray that returns a Set from an array
 * @param {Array} array - The source array.
 * @author Igbe Ajaga
 * @returns {Set}
 */
export default function setFromArray(array) {
	// returns a set of array
  return new Set(array);
}
